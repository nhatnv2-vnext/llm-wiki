packages:
  - "apps/*"

# Cho phép các package này chạy build script (postinstall) khi cài đặt.
# Tương đương việc duyệt qua `pnpm approve-builds`.
onlyBuiltDependencies:
  - esbuild
  - sharp
  - unrs-resolver
